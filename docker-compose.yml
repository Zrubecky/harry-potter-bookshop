version: '3.8'

services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - APP_PORT=${APP_PORT}
      - HOST=${HOST}
      - CORS_ORIGIN=${CORS_ORIGIN}
    volumes:
      - .:/app
    command: yarn start:prod